# Democracy Watcher - 環境変数
# このファイルを .env.local にリネームして、値を入れてください。

# Supabase Project URL（Settings → General → Project URL）
NEXT_PUBLIC_SUPABASE_URL=ここにProject URLを貼る

# Supabase Publishable Key（Settings → API Keys → Publishable key）
NEXT_PUBLIC_SUPABASE_ANON_KEY=ここにPublishable keyを貼る
